import { CustomstrucralDirective } from './customstrucral.directive';

describe('CustomstrucralDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomstrucralDirective();
    expect(directive).toBeTruthy();
  });
});
