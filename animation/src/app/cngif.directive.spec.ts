import { CngifDirective } from './cngif.directive';

describe('CngifDirective', () => {
  it('should create an instance', () => {
    const directive = new CngifDirective();
    expect(directive).toBeTruthy();
  });
});
