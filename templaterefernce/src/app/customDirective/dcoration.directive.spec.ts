import { DcorationDirective } from './dcoration.directive';

describe('DcorationDirective', () => {
  it('should create an instance', () => {
    const directive = new DcorationDirective();
    expect(directive).toBeTruthy();
  });
});
