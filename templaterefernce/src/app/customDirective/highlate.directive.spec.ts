import { HighlateDirective } from './highlate.directive';

describe('HighlateDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlateDirective();
    expect(directive).toBeTruthy();
  });
});
