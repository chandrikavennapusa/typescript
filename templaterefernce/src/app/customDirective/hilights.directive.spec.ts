import { HilightsDirective } from './hilights.directive';

describe('HilightsDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightsDirective();
    expect(directive).toBeTruthy();
  });
});
