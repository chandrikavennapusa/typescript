import { StructralDirective } from './structral.directive';

describe('StructralDirective', () => {
  it('should create an instance', () => {
    const directive = new StructralDirective();
    expect(directive).toBeTruthy();
  });
});
