import { AdddirecDirective } from './adddirec.directive';

describe('AdddirecDirective', () => {
  it('should create an instance', () => {
    const directive = new AdddirecDirective();
    expect(directive).toBeTruthy();
  });
});
